PORT=3002
COMPOSITOR_INTERNAL_SECRET=change-me-min-32-chars
COMPOSITOR_POOL_SIZE=1

# Recorder page Chromium loads. Must be loopback (Nest in this container).
# A public studio URL is ignored — that SPA does not expose __startRecording.
# COMPOSITOR_PAGE_ORIGIN=http://127.0.0.1:3002

# Recording sink the headless page connects to (same process/container).
# Use loopback — not ws://compositor (Docker DNS → private IP).
RECORDING_SINK_URL=ws://127.0.0.1:3002/ws/recording

# Direct SFU signaling for the headless compositor (required; page is not on the web origin).
# Local: ws://localhost:3001/ws/signaling
# Prod: wss://sfu.example.com/ws/signaling
SFU_PUBLIC_WS_URL=ws://localhost:3001/ws/signaling

# GPU: set to 1 to force GPU Chromium flags (needed on a Mac with no /dev/nvidia0).
# 0 forces SwiftShader/CPU. Unset = autodetect /dev/nvidia0 or /dev/dri.
# COMPOSITOR_GPU=1
# ANGLE backend on Linux. Default gl-egl (EGL, no X server). gl = GLX, never works in Docker.
# COMPOSITOR_ANGLE=gl-egl
# 1 = a failed GPU verdict kills the container instead of degrading to CPU.
# COMPOSITOR_GPU_STRICT=1
# COMPOSITOR_NVENC=0 disables ffmpeg NVENC even when the binary supports it.

# Optional
# FFMPEG_PATH=ffmpeg
# ffmpeg must support RTMPS (OpenSSL) for Facebook / Instagram Live Producer.
# PUPPETEER_EXECUTABLE_PATH=
