# Dev-only Postgres for local npm workflow.
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: streaming
      POSTGRES_PASSWORD: streaming
      POSTGRES_DB: streaming
    volumes:
      - streaming_pg:/var/lib/postgresql/data

volumes:
  streaming_pg:
